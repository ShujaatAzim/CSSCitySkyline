{
  "name": "CSSCitySkyline",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm run build && serve -s dist -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.4",
    "vite": "^6.3.5"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
